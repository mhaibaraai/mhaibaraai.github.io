/**
 * @category test
 * @description test function sum
 * @param a - the first number
 * @param b - the second number
 */
export function sum(a: number, b: number) {
  return a + b
}

/**
 * @category test
 * @description test function minus
 * @param a - the first number
 * @param b - the second number
 */
export function minus(a: number, b: number) {
  return a - b
}
